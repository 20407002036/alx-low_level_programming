#include <stdio.h>
/**
 * main - prints the numbers of arguments passed into it
 *
 * @argc: numbers of arguements passed to the main
 * @argv: Pointer to the array holding the arguements to main
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	int i = 0;


	if (argv[i] == 0)
	{
	 
	}
	printf("%d\n", argc - 1);

	return (0);
}
