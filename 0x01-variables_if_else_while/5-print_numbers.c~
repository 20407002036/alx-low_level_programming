#include <stdio.h>
/**
* main -Entry point
*
* Description: the function print all the single digit numbers of
* base 10.
*
* Return: Always 0 (Success)
*/
int main(void)
{
int n = 0;
for (n = 0 ; n < 10 ; n++)
{printf(n);
}
printf("\n");
return (0);
}
