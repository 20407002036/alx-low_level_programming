#include "main.h"
#include <stdio.h>
/**
 * reverse_array - reverses the contents of an array
 *
 * @a; pointer to the array
 * @n: size of the array
 *
 * Return: void
 */
void reverse_array(int *a, int n)
{
	int b[] ={};

	while (a != '\0')
	{
		a = b;
		a++;
	}
	b[] = '\0';

	*b = *a;
}
