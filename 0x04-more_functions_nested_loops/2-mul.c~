#include "main.h"
#include <stdio.h>
/**
* mul - the function multiplies two num
*
* @a: this is the 1st number. input
* @b: this is the 2nd input
*
* Return: multiplo of both
*/
int mul (int a, int b)
{ int mul;
mul = a * b;

return (mul);
}
